scalar Time

type User {
  id: ID!
  name: String!
  email: String!
  createdAt: Time!
}

type Auth {
  token: String!
  user: User!
}

input SignUpInput {
  name: String
  email: String!
  password: String!
  confirmPassword: String!
}

input LoginInput {
  email: String!
  password: String!
}

type Query {
  me: User!
}

type Mutation {
  signUp(input: SignUpInput!): Auth!
  login(input: LoginInput!): Auth!
}
